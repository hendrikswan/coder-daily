import React from 'react';
import Link from './Link';

const LinkList = () => {
    return (
        <div>
            <h4>this is the link list:</h4>

            <Link />
            <Link />
        </div>
    );
};

export default LinkList;
